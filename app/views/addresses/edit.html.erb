<!-- < %= form_tag "/profile/addresses/#{@address.id}", method: :patch do %> -->
  <!-- < %= text_field_tag :nickname, @address.nickname, placeholder: "Nickname"%> -->
  <!-- < %= text_field_tag :address, @address.address, placeholder: "Address"%> -->
  <!-- < %= text_field_tag :city, @address.city, placeholder: "City"%> -->
  <!-- < %= text_field_tag :state, @address.state, placeholder: "State"%> -->
  <!-- < %= text_field_tag :zip, @address.zip, placeholder: "Zip"%> -->
  <!-- < %= submit_tag "Submit" %> -->
<!-- < %end %> -->

<%= form_for @address, :url => update_address_url do |f| %>
  <%= f.text_field :nickname, placeholder: "Nickname"%>
  <%= f.text_field :address, placeholder: "Address"%>
  <%= f.text_field :city, placeholder: "City"%>
  <%= f.text_field :state, placeholder: "State"%>
  <%= f.text_field :zip, placeholder: "Zip"%>
  <%= f.submit "Submit" %>
<% end %>
